---
export const prerender = true;

import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Colleges and Universities in the United States - Rate My Professor Here',
};

import { states } from '~/data/states.ts';
---

<Layout metadata={metadata}>
  <div class="container p-8">
    <h3 class="text-2xl font-bold mb-8">Choose Your State</h3>

    <div class="grid gap-2">
      {
        states.map((state, index) => (
          <a
            href={`/state/colleges-in-${state.name.toLowerCase().replace(' ', '-')}/${state.id}`}
            class={`text-xl rounded-md  ${index % 2 == 0 && 'bg-gray-50'} shadow-sm overflow-hidden hover:shadow-md transition-shadow flex items-center p-2 text-center`}
          >
            <div>
              <p>{state.name}</p>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</Layout>
